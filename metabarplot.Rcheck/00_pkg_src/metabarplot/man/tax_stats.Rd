% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/barplot_funs.R
\name{tax_stats}
\alias{tax_stats}
\title{Build an Abundance Table from Taxonomy Data}
\usage{
tax_stats(
  tax_df,
  tax_rank,
  abund_column = NULL,
  method_column = "method",
  method_id = NULL,
  abund_tres = 0.01,
  sort_by_abund = TRUE,
  na_to_unknown = TRUE,
  unknown_label = "unknown",
  rare_label = "other (rare)"
)
}
\arguments{
\item{tax_df}{Taxonomy data frame with one column per taxonomic rank.}

\item{tax_rank}{Taxonomic rank column name in \code{tax_df}.}

\item{abund_column}{Optional abundance column in \code{tax_df}.}

\item{method_column}{Method/database column in \code{tax_df}.}

\item{method_id}{Sample identifier for output. Defaults to first value in
\code{method_column}.}

\item{abund_tres}{Taxa at or below this abundance are lumped into
\code{rare_label}. Set to \code{NA} to disable threshold-based lumping.}

\item{sort_by_abund}{If \code{TRUE}, taxa are sorted by abundance.}

\item{na_to_unknown}{If \code{TRUE}, \code{NA} taxa are replaced with \code{unknown_label}.}

\item{unknown_label}{Label for unknown taxa.}

\item{rare_label}{Label for low-abundance taxa.}
}
\value{
A tibble with columns for \code{tax_rank}, \code{Abundance}, and \code{Sample}.
}
\description{
Create an abundance table compatible with \code{\link[=pbar]{pbar()}} from a taxonomy table,
optionally weighted by an abundance column.
}
\examples{
x <- tibble::tibble(
  method = "demo",
  Genus = c("A", "B", NA),
  reads = c(80, 15, 5)
)
tax_stats(x, tax_rank = "Genus", abund_column = "reads")
}
