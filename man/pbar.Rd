% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/barplot_funs.R
\name{pbar}
\alias{pbar}
\title{Create Taxonomic Abundance Barplots}
\usage{
pbar(
  ps = NULL,
  taxrank = "Phylum",
  axis_var = "Sample",
  facet_var = NULL,
  facet_var2 = NULL,
  axis_lab = NULL,
  abund_tres = 0.01,
  focal_taxa = NULL,
  na_to_unknown = TRUE,
  sort_by_abund = TRUE,
  colors = cols_kelly,
  abund_df = NULL,
  convert_abund = FALSE,
  return_abund = FALSE,
  unknown_label = "unknown",
  rare_label = "other (rare)",
  alpha = 1,
  facet_scales = "free"
)
}
\arguments{
\item{ps}{A \code{phyloseq} object. Required when \code{abund_df} is \code{NULL}.}

\item{taxrank}{Taxonomic rank to summarize abundance by.}

\item{axis_var}{Variable to plot on the y-axis. Use \code{"Sample"} for
per-sample bars, or a metadata column name.}

\item{facet_var}{Optional metadata column for faceting rows.}

\item{facet_var2}{Optional second metadata column for faceting columns.}

\item{axis_lab}{Axis title for the y-axis.}

\item{abund_tres}{Taxa with mean abundance below this threshold are lumped
into \code{rare_label}. Set to \code{NA} to disable threshold-based lumping.}

\item{focal_taxa}{Optional vector of taxa to keep. Non-focal taxa are lumped
into \code{rare_label}.}

\item{na_to_unknown}{If \code{TRUE}, missing taxon labels are converted to
\code{unknown_label}.}

\item{sort_by_abund}{If \code{TRUE}, taxa are ordered by mean abundance.}

\item{colors}{Optional vector of colors. If \code{NULL}, a distinct palette is
generated.}

\item{abund_df}{Optional abundance table, typically produced by
\code{\link[=abund_stats]{abund_stats()}}. Must include \code{Abundance}, \code{Sample}, and \code{taxrank} columns.}

\item{convert_abund}{If \code{TRUE}, convert per-sample counts in \code{ps} to relative
abundances.}

\item{return_abund}{If \code{TRUE}, return the abundance table and skip plotting.}

\item{unknown_label}{Label to use for unknown taxa.}

\item{rare_label}{Label to use for lumped low-abundance taxa.}

\item{alpha}{Opacity of fill colors.}

\item{facet_scales}{Facet scale setting passed to \code{ggh4x::facet_grid2()}.}
}
\value{
A \code{ggplot2} object by default, or a tibble if \code{return_abund = TRUE}.
}
\description{
Build a stacked abundance barplot from either a \code{phyloseq} object or a
precomputed abundance table.
}
\examples{
abund_df <- tibble::tibble(
  Sample = c("S1", "S1", "S2", "S2"),
  Phylum = c("Firmicutes", "Proteobacteria", "Firmicutes", "Proteobacteria"),
  Abundance = c(0.4, 0.6, 0.7, 0.3)
)
p <- pbar(abund_df = abund_df, taxrank = "Phylum")
p
}
